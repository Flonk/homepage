---
---

<div class="sample">
	<h4>A heading, which wears its own hashes</h4>
	<p class="mono">
		The mono voice. Nav, headings, the post index, code — anything the site
		says about itself rather than to you.
	</p>
	<p class="serif">
		And the prose voice, which is Source Serif 4 and self-hosted. This is what
		a post body is set in, and this post is one.
	</p>
	<blockquote>A quote takes the accent down its left edge.</blockquote>
</div>

<style>
	.sample p {
		max-width: 62ch;
	}

	.mono {
		font-family: var(--font-mono);
		font-size: 0.875rem;
		line-height: 1.7;
	}

	.serif {
		font-family: var(--font-prose);
	}
</style>
