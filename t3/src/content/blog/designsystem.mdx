---
title: "Design system"
description: "The site's tokens and patterns, on one page."
pubDate: "August 25 2026"
hidden: true
---

import FlbBar from "../../components/ds/FlbBar.astro";
import FlbSolids from "../../components/ds/FlbSolids.astro";
import GlassDemo from "../../components/ds/GlassDemo.astro";
import PillDemo from "../../components/ds/PillDemo.astro";
import RadiusScale from "../../components/ds/RadiusScale.astro";
import Swatches from "../../components/ds/Swatches.astro";
import TrackDemo from "../../components/ds/TrackDemo.astro";

## Rules
- Use OKLCH for everything.
- Any border on an object should be an *FLB* (fake luminosity border).
- Do not clutter this page with text; AI agents will read the code anyway.

## Colour

<Swatches />

## Radius

<RadiusScale />

## FLB

Any border on an object should be an *FLB* (fake luminosity border). If the element is a flat color, its border can be a real CSS border; same color but 20% lighter.

<FlbSolids />

If the content is a gradient or some other sort of non-solid colour, the border
must be emulated to show a fake 1px ring of the same colour, 20% lighter,
around the element.

<FlbBar />

## Components

### VizTrack

<TrackDemo />

### Pill

<PillDemo />

### Glass

<GlassDemo />
