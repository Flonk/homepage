---
title: "Colour #4: Pink, Olo and Other Impossible Colors"
series: "Colour"
format: "deck"
ground: "dark"
description: ""
pubDate: "August 21 2026"
---

import DeckCard from "../../components/DeckCard.astro";
import DeckViz from "../../components/DeckViz.astro";
import ConeResponse from "../../components/ConeResponse.astro";
import VizPhase from "../../components/VizPhase.astro";
import VizStill from "../../components/VizStill.astro";
import LightMix from "../../components/LightMix.astro";

{/* The prism first, then part three's graph, carried over because this post
    picks up exactly where that one left off: the curves are what say which
    cone signals light can and cannot produce, and olo is one of the ones it
    cannot. A viz of its own — the space of possible signals, with the
    unreachable part marked — is the thing to build here when the prose knows
    what it wants. The first phase listed is the one a deck opens on. */}
<DeckViz>
  <VizPhase name="prism">
    <VizStill
      src="/assets/2026-08-21-color-4-olo/prism.png"
      alt="A beam of white light entering a glass prism and leaving it spread into a band of colour."
      credit="George Hardie, 1973"
    />
  </VizPhase>
  <VizPhase name="mix">
    {/* 600, 470 and 560nm at these shares come to #ff9fc4, which is as
        ordinary a pink as there is. Move any of them and the mixture moves
        with it; the reading underneath says how far the result is from
        anything the spectrum can do on its own. */}
    <LightMix
      lights={[
        { nm: 600, weight: 1 },
        { nm: 470, weight: 0.6 },
        { nm: 560, weight: 0.3 },
      ]}
    />
  </VizPhase>
  <VizPhase name="cones">
    <ConeResponse sweep={[400, 700]} drag />
  </VizPhase>
</DeckViz>

<DeckCard>
  ## The real world isn't single wavelength

  The sun, being a massive ball of plasma, among basically any other white light source, will be emitting photons across the entire visible spectrum -- whatever reaches our eye at any given moment will rarely be a single wavelength of photons, meaning we really need to look at the *combined* effect of multiple wavelengths hitting our cones.

  This will create situations in which cones are stimulated in ways that single wavelength light cannot, leading to us seeing *non-spectral* colors.
</DeckCard>

{/* Part three's curves, to look at combined stimulation on. The light
    mixer is still wired up as the "mix" phase if it turns out to be wanted
    here. */}
<DeckCard vizProps={{ phase: "cones" }}>
  ## Pink is not a wavelength

  Bla di bla
</DeckCard>
